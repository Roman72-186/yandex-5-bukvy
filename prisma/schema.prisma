// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Looking for ways to speed up your queries, or scale easily with your serverless or edge functions?
// Try Prisma Accelerate: https://pris.ly/cli/accelerate-init

generator client {
  provider = "prisma-client-js"
  output   = "../lib/generated/prisma"
}

datasource db {
  provider = "sqlite"
}

model WordDictionary {
  id        Int        @id @default(autoincrement())
  word      String     @unique // Assuming max 5 characters for a 5-letter word
  dailyWord DailyWord[] // Relation to DailyWord

  @@map("word_dictionary")
}

model DailyWord {
  id        Int            @id @default(autoincrement())
  date      DateTime       @unique // Unique date for the daily word
  wordId    Int            @unique
  word      WordDictionary @relation(fields: [wordId], references: [id], onDelete: Restrict, onUpdate: Cascade)

  @@map("daily_word")
}
